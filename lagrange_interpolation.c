/*
 *    Programme to emulate the Lagrange Interpolation formula for a
 *    given function F(x), we are finding out the coressponding values
 *
*/

#include<stdio.h>
#include<stdlib.h>

void omega(int x0, int x1, int x2, int x3);

int main(int argc, char **argv)
{
	if(argc != 5)
	{
		printf("Usage: </programe> <x0> <x1> <x2> <x3>");
	}
	exit(0);
}
